final class AppConstants {
  const AppConstants._();

  static const String fontFamily = 'Rubik';
  static const Duration searchIconCloseAnimDuration = Duration(
    milliseconds: 300,
  );
  static const Duration switchAnimDuration = Duration(
    milliseconds: 150,
  );
}
